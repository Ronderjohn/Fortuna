"""Phase 2 reinforcement learning package.

Subpackages:
    - ``env``      : Gymnasium env wrapping Phase 1 walk-forward folds.
    - ``training`` : PPO trainer + callbacks + checkpoint serialization.
    - ``inference``: live signal generator + regime detector.
    - ``models``   : custom policy extractors (CNN, ...).
"""
